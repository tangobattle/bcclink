//! Library surface of Ring, exposed so the harness examples (e.g.
//! `examples/selftest.rs`) can drive the same hooks and link the app uses.

pub mod audio;
pub mod emu;
pub mod hooks;
pub mod link;
pub mod net;
